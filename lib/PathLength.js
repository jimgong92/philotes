/**
 * getAveragePathLength
 *
 * Gets the average path length of the network, or of the specified node to
 * the rest of the network if provided
 *
 * @param network [Object] - Shape of network with nodes key and links key
 * where each key in the nodes value object is a nodeId and each link in the
 * links value object is a 
 * e.g. 
 * {
 *   node1: {
 *     edges: {
 * 
 *     },
 *
 *   }
 *   ...
 * }
 * @param nodeId [string] - OPTIONAL
 */
function getAveragePathLength(network, nodeId) {

}

module.exports = getAveragePathLength;
